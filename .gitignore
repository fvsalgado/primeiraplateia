# Dados gerados em runtime — nunca commitar
# O pipeline gera data/ em cada run e inclui-o no deploy via _site/
data/

# Pasta de deploy temporária (criada pelo workflow, nunca commitar)
_site/

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/
.env

# macOS
.DS_Store
._*

# Windows
Thumbs.db

# IDEs
.idea/
.vscode/
*.swp

# Logs
*.log
scraper_run.log
scraper_run.jsonl

# Cache por teatro (gerado pelo scraper.py — não commitar)
scrapers/cache/*.json

# Pytest
.pytest_cache/
